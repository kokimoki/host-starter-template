/**
 * Actions for managing the players list.
 *
 * This is a placeholder for consumer apps to add player-related mutations.
 * Add actions here for operations like updating scores, assigning teams,
 * removing players, or any other player registry modifications.
 *
 * @example
 * ```ts
 * export const playersActions = {
 *   async updateScore(playerId: string, score: number) {
 *     await kmClient.transact([playersStore], ([state]) => {
 *       state.players[playerId].score = score;
 *     });
 *   }
 * };
 * ```
 *
 * @see localPlayerActions.setPlayerName for player registration example
 * @see playersStore for the players registry store
 */
export const playersActions = {};
